agents:
  queue: central
  slurm_mem: 8G
  modules: climacommon/2025_03_18

env:
  JULIA_LOAD_PATH: "${JULIA_LOAD_PATH}:${BUILDKITE_BUILD_CHECKOUT_PATH}/test"
  OPENBLAS_NUM_THREADS: 1
  JULIA_NVTX_CALLBACKS: gc
  OMPI_MCA_opal_warn_on_missing_libcuda: 0
  JULIA_MAX_NUM_PRECOMPILE_FILES: 100

steps:
  - label: "init cpu env"
    key: "init_cpu_env"
    command:

      - echo "--- Instantiate test"
      - "rm -rf test/Manifest.toml"
      - "julia --project=test/ -e 'using Pkg; Pkg.instantiate(;verbose=true)'"
      - "julia --project=test/ -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate(;verbose=true); Pkg.precompile(;strict=true)'"

      - echo "--- Instantiate perf"
      - "julia --project=perf -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate(;verbose=true); Pkg.precompile()'"

      - echo "--- Package status"
      - "julia --project=test/ -e 'using Pkg; Pkg.status()'"


  - label: "init gpu env"
    key: "init_gpu_env"
    depends_on: "init_cpu_env"
    command:
      - echo "--- Initialize CUDA runtime"
      - "julia --project=test/ -e 'using CUDA; CUDA.precompile_runtime(); CUDA.versioninfo()'"

      - echo "--- Package status"
      - "julia --project=test/ -e 'using Pkg; Pkg.status()'"
    agents:
      slurm_gres: "gpu:1"

  - wait

  # ================================================================== #
  # Unit tests (fast, no convergence studies)
  # ================================================================== #
  - group: "Unit tests"
    key: unit_tests
    steps:

      - label: "unit: sparse_containers"
        command: "julia --project=test/ --check-bounds=yes test/unit/sparse_containers.jl"

      - label: "unit: fused_increment"
        command: "julia --project=test/ --check-bounds=yes test/unit/fused_increment.jl"

      - label: "unit: tableaus"
        command: "julia --project=test/ --check-bounds=yes test/unit/tableaus.jl"

      - label: "unit: update_signal_handler"
        command: "julia --project=test/ --check-bounds=yes test/unit/update_signal_handler.jl"

      - label: "unit: line_search"
        command: "julia --project=test/ --check-bounds=yes test/unit/line_search.jl"

      - label: "unit: convergence_checker"
        command: "julia --project=test/ --check-bounds=yes test/utils/convergence_checker.jl"

      - label: "unit: forward_euler"
        command: "julia --project=test/ --check-bounds=yes test/unit/forward_euler.jl"

      - label: "unit: incrementing_ode"
        command: "julia --project=test/ --check-bounds=yes test/unit/incrementing_ode.jl"

      - label: "unit: edge_cases"
        command: "julia --project=test/ --check-bounds=yes test/unit/edge_cases.jl"

      - label: "unit: jacobian_accuracy"
        command: "julia --project=test/ --check-bounds=yes test/unit/jacobian_accuracy.jl"

      - label: "unit: aqua"
        command: "julia --project=test/ --check-bounds=yes test/aqua.jl"

  # ================================================================== #
  # Solver correctness
  # ================================================================== #
  - group: "Solver correctness"
    key: solver_correctness
    steps:

      - label: "solver: newtons_method"
        command: "julia --project=test/ --check-bounds=yes test/solvers/newtons_method.jl"

  # ================================================================== #
  # Integration / system-level tests
  # ================================================================== #
  - group: "Integration tests"
    key: integration_tests
    steps:

      - label: "integration: single_column_ars"
        command: "julia --project=test/ --check-bounds=yes test/integration/single_column_ars.jl"

      - label: "integration: callbacks (CPU)"
        command: "julia --project=test/ --check-bounds=yes test/integration/callbacks.jl"

      - label: "integration: callbacks (MPI)"
        command: "julia --project=test/ --check-bounds=yes test/integration/callbacks.jl"
        env:
          CLIMACOMMS_CONTEXT: "MPI"

      - label: "integration: integrator"
        command: "julia --project=test/ --check-bounds=yes test/integration/integrator.jl"

      - label: "integration: limiter"
        command: "julia --project=test/ --check-bounds=yes test/integration/limiter.jl"

      - label: "integration: second_newton"
        command: "julia --project=test/ --check-bounds=yes test/integration/second_newton.jl"

      - label: "integration: ssp_monotonicity"
        command: "julia --project=test/ --check-bounds=yes test/integration/ssp_monotonicity.jl"

      - label: "integration: dense_output"
        command: "julia --project=test/ --check-bounds=yes test/integration/dense_output.jl"

      - label: "integration: long_time_stability"
        command: "julia --project=test/ --check-bounds=yes test/integration/long_time_stability.jl"

  # ================================================================== #
  # Performance / allocation tests
  # ================================================================== #
  - group: "Performance tests"
    key: performance_tests
    steps:

      - label: "perf: type_stability"
        command: "julia --project=test/ --check-bounds=yes test/performance/type_stability.jl"

      - label: "perf: allocations"
        command: "julia --project=test/ --check-bounds=yes test/performance/allocations.jl"

  # ================================================================== #
  # Convergence order tests (parallelized across solvers)
  # ================================================================== #
  - group: "Convergence tests"
    key: convergence_tests
    steps:

      - label: "conv: explicit_rk"
        command: "julia --project=test/ --check-bounds=yes test/solvers/explicit_rk.jl"

      - label: "conv: lsrk"
        command: "julia --project=test/ --check-bounds=yes test/solvers/lsrk.jl"

      - label: "conv: multirate"
        command: "julia --project=test/ --check-bounds=yes test/solvers/multirate.jl"

      - label: "conv: rosenbrock"
        command: "julia --project=test/ --check-bounds=yes test/solvers/rosenbrock.jl"

      - label: "conv: imex_ark"
        command: "julia --project=test/ --check-bounds=yes test/solvers/imex_ark.jl"

  # ================================================================== #
  # GPU tests
  # ================================================================== #
  - group: "GPU tests"
    key: gpu_tests
    steps:

      - label: "GPU: runtests"
        command: "julia --project=test/ --check-bounds=yes test/runtests.jl CuArray"
        artifact_paths: "output/*"
        agents:
          slurm_gres: "gpu:1"

  # ================================================================== #
  # Profiling / benchmarks
  # ================================================================== #
  - group: "Profiling"
    key: profiling
    steps:

      - label: "Perf: Flame graph (1D diffusion)"
        command: "julia --project=perf perf/flame.jl --job_id diffusion_1D"
        artifact_paths: "diffusion_1D/*"

      - label: "Perf: Jet (IMEX ARK system)"
        command: "julia --project=perf perf/jet.jl --problem ark_sys"

      - label: "Perf: Jet (FD diffusion 2D)"
        command: "julia --project=perf perf/jet.jl --problem diffusion2d"

      - label: "Perf: Benchmark"
        command: "julia --project=perf perf/benchmark.jl"
